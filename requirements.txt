# ── Core ML dependencies ─────────────────────────────────────────────────────
# CPU-only torch (~500MB) — sufficient for inference, avoids OOM on free tiers
--extra-index-url https://download.pytorch.org/whl/cpu
torch==2.2.2+cpu
torchaudio==2.2.2+cpu

# ── Vision & Audio ────────────────────────────────────────────────────────────
numpy>=1.26,<2
# Headless variant — no display server required on Linux/cloud servers
opencv-python-headless>=4.9,<5
facenet-pytorch>=2.6.0
librosa>=0.10
soundfile>=0.12
moviepy>=1.0
Pillow>=10.2,<11

# ── API Backend ───────────────────────────────────────────────────────────────
fastapi>=0.104
uvicorn[standard]>=0.24
python-multipart>=0.0.6
aiofiles>=23.2

# ── Data & Utils ─────────────────────────────────────────────────────────────
PyYAML>=6.0
pandas>=1.5
requests>=2.28

# ── UI (Streamlit alternative frontend) ──────────────────────────────────────
streamlit>=1.32
plotly>=5.17

# ── Dev / Testing ─────────────────────────────────────────────────────────────
pytest>=8.0
